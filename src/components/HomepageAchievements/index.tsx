import clsx from "clsx";
import styles from './styles.module.css';

export default function HomepageAchievements(): JSX.Element {
    return (
        <section className={styles.achievements}>
            <div className="container text--center">
            </div>
        </section>
    );
}